# Telegram Bot - Орден Демиургов

Бот для управления подписками на каналы "Орден Демиургов" и "Родители Демиурги".

## 📋 Содержание

- [Установка](#установка)
- [Структура проекта](#структура-проекта)
- [Описание файлов](#описание-файлов)
- [Функционал](#функционал)
- [Админ-панель](#админ-панель)

## 🚀 Установка

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

2. Скопируйте `env_example.txt` в `.env` и заполните все необходимые параметры:

```bash
cp env_example.txt .env
```

3. Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE demiurg_bot;
```

4. Запустите бота:

```bash
python main.py
```

## 📁 Структура проекта

```
Orden_demiurgov/
├── main.py                    # Точка входа, запуск бота
├── config.py                  # Конфигурация и настройки
├── database.py                # Работа с базой данных PostgreSQL
├── handlers.py                # Обработчики команд и callback'ов (включая админ-панель)
├── keyboards.py               # Клавиатуры бота
├── messages.py                # Тексты всех сообщений
├── robokassa.py               # Интеграция с Robokassa (генерация URL платежей)
├── payment_handler.py         # Обработка webhook'ов от Robokassa
├── scheduler.py               # Планировщик для автоматических задач
├── requirements.txt           # Зависимости проекта
├── env_example.txt            # Пример файла с переменными окружения
├── database_schema.sql        # SQL схема базы данных
├── database_schema_diagram.txt # Визуальная схема БД
├── DATABASE_DOCUMENTATION.md  # Подробная документация по БД
├── SETUP.md                   # Инструкция по настройке
└── MIGRATION_TO_POSTGRES.md   # Инструкция по миграции на PostgreSQL
```

## 📄 Описание файлов

### 🎯 `main.py` - Точка входа и запуск бота

**Ответственность:**

- Инициализация бота и диспетчера
- Создание пула соединений к PostgreSQL при запуске
- Запуск webhook-сервера для обработки платежей (порт 8080)
- Запуск планировщика для автоматических задач
- Запуск polling для получения обновлений от Telegram

**Ключевые функции:**

- `on_startup()` - инициализация БД и планировщика при запуске
- `main()` - основная функция запуска бота

---

### ⚙️ `config.py` - Конфигурация и настройки

**Ответственность:**

- Загрузка переменных окружения из `.env` файла
- Хранение всех конфигурационных параметров:
  - Токен бота
  - ID администраторов
  - Настройки Robokassa
  - Настройки PostgreSQL
  - ID каналов
  - Цены и периоды подписок
  - Ссылки (поддержка, оферта)

**Использование:**
Все модули импортируют константы из этого файла для доступа к настройкам.

---

### 🗄️ `database.py` - Работа с базой данных

**Ответственность:**

- Управление пулом соединений к PostgreSQL
- Все операции с базой данных:
  - Работа с пользователями (добавление, получение, обновление)
  - Управление подписками (создание, активация, деактивация)
  - Работа с платежами (создание, обновление статуса)
  - Управление напоминаниями
- Инициализация таблиц и индексов при первом запуске

**Ключевые методы:**

- `init_db()` - создание пула соединений и инициализация таблиц
- `add_user()` - добавление/обновление пользователя
- `create_subscription()` - создание подписки
- `create_payment()` - создание записи о платеже
- `get_expired_subscriptions()` - получение истекших подписок
- И другие методы для работы с БД

**Подключение к БД:**

- Пул соединений создается в `main.py` при вызове `db.init_db()`
- Каждый метод получает соединение из пула через `self.pool.acquire()`
- Соединение автоматически возвращается в пул после использования

---

### 💬 `handlers.py` - Обработчики команд и callback'ов

**Ответственность:**

- Обработка всех команд от пользователей (`/start`, `/admin`, `/import_users`)
- Обработка всех callback'ов от кнопок (главное меню, оплата, подписки и т.д.)
- Логика взаимодействия с пользователями
- **АДМИН-ПАНЕЛЬ** (строки 223-294):
  - `/admin` - вход в админ-панель, показывает доступные команды
  - `/import_users` - импорт пользователей из мастер-класса с выдачей подарка

**Основные обработчики:**

**Для обычных пользователей:**

- `cmd_start()` - обработка команды `/start`, проверка подарка, показ главного меню
- `callback_main_menu()` - главное меню
- `callback_channel_1_info()` / `callback_channel_2_info()` - информация о каналах
- `callback_my_subscriptions()` - просмотр подписок пользователя
- `callback_payment()` - обработка нажатия кнопки "Оплатить"
- `callback_legal_info()` - юридическая информация

**Для администраторов:**

- `cmd_admin()` - **АДМИН-ПАНЕЛЬ** (строка 224)
  - Проверяет, является ли пользователь администратором
  - Показывает доступные команды
- `cmd_import_users()` - **ИМПОРТ ПОЛЬЗОВАТЕЛЕЙ** (строка 238)
  - Проверяет права администратора
  - Принимает список telegram_id через пробел
  - Импортирует пользователей и выдает им подарок (2 недели бесплатного доступа)

**Где находится админ-панель:**
📍 **Файл:** `handlers.py`  
📍 **Строки:** 223-294  
📍 **Команды:**

- `/admin` - вход в админ-панель (строка 224)
- `/import_users 123456789 987654321` - импорт пользователей (строка 238)

---

### ⌨️ `keyboards.py` - Клавиатуры бота

**Ответственность:**

- Создание всех inline-клавиатур для бота
- Определение структуры кнопок и их callback_data

**Функции:**

- `get_main_menu_keyboard()` - главное меню с кнопками каналов
- `get_payment_keyboard()` - кнопки для оплаты
- `get_reminder_keyboard()` - кнопки в напоминании (за 3 дня до окончания)
- `get_expired_keyboard()` - кнопки при истечении подписки
- `get_back_to_main_keyboard()` - кнопка "На главную"
- `get_legal_info_keyboard()` - кнопки в юридической информации

---

### 📝 `messages.py` - Тексты сообщений

**Ответственность:**

- Хранение всех текстов сообщений, которые отправляет бот
- Форматирование дат для отображения

**Основные функции:**

- `get_start_message()` - приветственное сообщение для обычных пользователей
- `get_gift_welcome_message()` - сообщение о подарке для пользователей из мастер-класса
- `get_channel_1_info_message()` / `get_channel_2_info_message()` - описание каналов
- `get_reminder_message()` - напоминание за 3 дня до окончания
- `get_expired_message()` - сообщение об истечении подписки
- `get_payment_success_message()` - сообщение об успешной оплате
- `get_payment_success_with_bonus_message()` - сообщение об оплате с бонусом
- `get_subscriptions_message()` - информация о подписках пользователя
- `get_legal_info_message()` - юридическая информация

---

### 💳 `robokassa.py` - Интеграция с Robokassa

**Ответственность:**

- Генерация URL для оплаты через Robokassa
- Создание подписи (signature) для платежей
- Проверка подписи от Robokassa

**Функции:**

- `generate_payment_url()` - создание URL для перехода к оплате
- `verify_payment_signature()` - проверка подписи от Robokassa
- `get_result_url_signature()` - генерация подписи для ResultURL

---

### 🔔 `payment_handler.py` - Обработка webhook'ов от Robokassa

**Ответственность:**

- Обработка уведомлений от Robokassa о платежах
- Проверка подписи платежа
- Обновление статуса платежа в БД
- Активация подписки после успешной оплаты

**Обработчики:**

- `robokassa_result_handler()` - обработка ResultURL (уведомление от Robokassa)
- `robokassa_success_handler()` - обработка SuccessURL (редирект после оплаты)
- `robokassa_fail_handler()` - обработка FailURL (редирект при ошибке)

**Маршруты:**

- `POST /robokassa/result` - уведомление от Robokassa
- `GET /robokassa/success` - успешная оплата
- `GET /robokassa/fail` - неудачная оплата

---

### ⏰ `scheduler.py` - Планировщик автоматических задач

**Ответственность:**

- Автоматическая проверка и отправка напоминаний
- Автоматическая деактивация истекших подписок
- Удаление пользователей из каналов при истечении подписки

**Задачи:**

- `check_reminders()` - проверка неотправленных напоминаний (каждый час)
- `check_expired_subscriptions()` - проверка истекших подписок (каждые 6 часов)

**Использование:**
Планировщик запускается автоматически при старте бота в `main.py`.

---

### 📦 `requirements.txt` - Зависимости проекта

**Ответственность:**

- Список всех Python-пакетов, необходимых для работы бота

**Основные зависимости:**

- `aiogram` - библиотека для работы с Telegram Bot API
- `asyncpg` - асинхронный драйвер для PostgreSQL
- `apscheduler` - планировщик задач
- `aiohttp` - асинхронный HTTP-клиент/сервер
- `python-dotenv` - загрузка переменных окружения

---

### 📄 Документация

**`DATABASE_DOCUMENTATION.md`** - Подробная документация по базе данных:

- Описание всех таблиц и полей
- Где происходит подключение к БД
- Связи между таблицами
- Индексы и оптимизация

**`SETUP.md`** - Инструкция по настройке бота:

- Настройка переменных окружения
- Настройка Robokassa
- Настройка каналов
- Развертывание на сервере

**`MIGRATION_TO_POSTGRES.md`** - Инструкция по миграции на PostgreSQL:

- Создание базы данных
- Миграция данных из SQLite (если нужно)

**`database_schema.sql`** - SQL скрипт для создания таблиц

**`database_schema_diagram.txt`** - Визуальная схема базы данных в текстовом виде

---

## ✨ Функционал

### Для пользователей:

- ✅ Регистрация при первом обращении (`/start`)
- ✅ Просмотр информации о каналах
- ✅ Покупка подписки через Robokassa
- ✅ Просмотр своих подписок
- ✅ Автоматическое получение подарка (2 недели бесплатно) при оплате "Родители Демиурги" (если не было подписки на "Орден Демиургов")
- ✅ Автоматические напоминания за 3 дня до окончания подписки
- ✅ Получение уведомлений об истечении подписки

### Для пользователей из мастер-класса:

- ✅ Автоматическое получение подарка (2 недели бесплатного доступа) при импорте администратором
- ✅ Автоматическое добавление в канал "Орден Демиургов"
- ✅ Напоминание за 3 дня до окончания подарка

### Для администраторов:

- ✅ Админ-панель (`/admin`)
- ✅ Импорт пользователей из мастер-класса (`/import_users`)

---

## 👨‍💼 Админ-панель

### Где находится админ-панель:

**Файл:** `handlers.py`  
**Строки:** 223-294

### Команды:

#### 1. `/admin` - Вход в админ-панель

**Строка:** 224

Показывает доступные команды администратора:

```
Админ-панель

Доступные команды:
/import_users - Импорт пользователей из мастер-класса
Формат: /import_users 123456789 987654321 111222333
```

#### 2. `/import_users` - Импорт пользователей из мастер-класса

**Строка:** 238

**Использование:**

```
/import_users 123456789 987654321 111222333
```

**Что делает:**

1. Проверяет права администратора
2. Парсит список telegram_id из команды
3. Проверяет каждого пользователя:
   - Если пользователь не существует - создает его
   - Если пользователь существует, но не получил подарок - добавляет в список
4. Для каждого пользователя, который должен получить подарок:
   - Создает подписку на 14 дней (подарок)
   - Добавляет пользователя в канал "Орден Демиургов"
   - Создает напоминание на день 11 (за 3 дня до окончания)
   - Отправляет приветственное сообщение с подарком
5. Возвращает статистику импорта

**Проверка прав:**
Админ-панель доступна только пользователям, чьи ID указаны в переменной окружения `ADMIN_IDS` в файле `.env`.

**Пример настройки:**

```env
ADMIN_IDS=123456789,987654321
```

---

## 🔄 Поток работы бота

### Обычный пользователь:

1. Нажимает `/start` → бот проверяет, есть ли подарок
2. Видит главное меню
3. Выбирает канал → видит описание и цену
4. Нажимает "Оплатить" → переходит на Robokassa
5. После оплаты → получает доступ к каналу
6. За 3 дня до окончания → получает напоминание
7. При истечении → получает уведомление

### Пользователь из мастер-класса:

1. Администратор импортирует через `/import_users`
2. Пользователь автоматически получает подарок (2 недели)
3. Автоматически добавляется в канал
4. На день 11 получает напоминание
5. Может оплатить или подписка истечет

### Администратор:

1. Нажимает `/admin` → видит доступные команды
2. Использует `/import_users` для импорта пользователей
3. Получает статистику импорта

---

## 📚 Дополнительная документация

- **Подробная документация по БД:** `DATABASE_DOCUMENTATION.md`
- **Инструкция по настройке:** `SETUP.md`
- **Миграция на PostgreSQL:** `MIGRATION_TO_POSTGRES.md`
- **SQL схема:** `database_schema.sql`
- **Визуальная схема БД:** `database_schema_diagram.txt`
